kind = "web"
previewPath = "/"
title = "NetMon — Network Monitor"
version = "1.0.0"
id = "artifacts/netmon"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 20885

[services.development]
run = "pnpm --filter @workspace/netmon run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/netmon", "run", "build" ]
publicDir = "artifacts/netmon/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "20885"
BASE_PATH = "/"
